apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "awi-catalyst-sdwan-k8s-operator.fullname" . }}-metrics-reader
  labels:
  {{- include "awi-catalyst-sdwan-k8s-operator.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get